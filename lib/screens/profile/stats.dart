import 'package:flutter/material.dart';

class StatsSubSection extends StatelessWidget {
  const StatsSubSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
